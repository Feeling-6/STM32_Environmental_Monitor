# AGENTS.md — 给 AI 助手的项目说明

> 这份文件是写给**下一个 AI 会话**的。项目里踩过的坑密度很高，丢了会重踩一遍，
> 甚至改坏已经上板验证过的代码。**动手前请先读完「绝不能踩的坑」一节。**

---

## 1. 项目性质

| 项       | 值                                                                     |
| -------- | ---------------------------------------------------------------------- |
| 平台     | PlatformIO                                                             |
| 框架     | `framework = cmsis` + **ST 标准外设库 v3.6.0**（`lib/SPL/`，vendored） |
| **不是** | **不是 HAL、不是 Arduino、不是 CubeMX 生成、没有 FreeRTOS**            |
| 芯片     | STM32F103C8T6，HCLK 72MHz，PCLK1 36MHz，PCLK2 72MHz                    |
| 资源     | 64KB Flash / 20KB RAM（当前占用约 6.8KB / 1.2KB）                      |
| 烧录     | ST-Link（`upload_protocol = stlink`），SWD                             |
| 参考来源 | 江协科技标准库教程。但驱动是重写的，不是照抄                           |

`platformio.ini` 的编译宏：`USE_STDPERIPH_DRIVER`、`STM32F10X_MD`，包含路径 `include` 和 `lib/SPL/include`。

---

## 2. 构建与烧录

**`pio` 不在 PATH 里，必须用全路径：**

```bash
~/.platformio/penv/Scripts/pio.exe run              # 编译
~/.platformio/penv/Scripts/pio.exe run -t upload    # 烧录
~/.platformio/penv/Scripts/pio.exe run -t clean     # 清理
```

符号/体积检查：

```bash
NM="/c/Users/Feeling/.platformio/packages/toolchain-gccarmnoneeabi/bin/arm-none-eabi-nm.exe"
"$NM" -S --size-sort .pio/build/genericSTM32F103C8/firmware.elf
```

---

## 3. 代码规范（照现有文件来，别自创风格）

- **UTF-8 编码**（教程源码是 GBK，**不要直接粘字节**，会变乱码）
- **Tab 缩进**（教程是 4 空格，本项目统一 Tab）
- **中文注释**
- 头文件守卫：`#ifndef __XXX_H`
- 命名：`模块_动作`，PascalCase 模块名 —— `OLED_ShowString`、`I2C2_BusInit`、`Key_GetEvent`、`AD_GetMQ2`。**不是 snake_case**
- 公开函数用中文 Doxygen 块：

  ```c
  /**
    * @brief  OLED显示一个字符
    * @param  Line 行位置，范围：1~4
    * @retval 无
    * @note   这里写非显然的约束或坑
    */
  ```
- 行尾注释用 Tab 与代码隔开：`//开启GPIOB的时钟`
- 初始化函数内用 `/*section*/` 分节：`/*开启时钟*/`、`/*GPIO初始化*/`

---

## 4. ⚠️ 绝不能踩的坑

**这一节是本文档的核心。每条都能在源码里指到出处。**

### 4.1 命名冲突

| 坑                     | 后果                                                                                            | 出处                                                  |
| ---------------------- | ----------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| **模块不能叫 `ADC_*`** | `ADC_Init` 是 SPL 已有函数（`stm32f10x_adc.h:428`），同名会**编译不过**。同理 `ADC_Get*` 全被占 | 所以模拟量模块叫 **`AD_`**（`AD_Init` / `AD_GetMQ2`） |

### 4.2 I2C

| 坑                                                | 后果                                                                                                                               | 出处                                                                                                               |
| ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| **`I2C_Send7bitAddress` 必须传左移后的 8 位地址** | 该函数**只改 bit0** 然后把整字节写 DR，从不左移。传 7 位原值会寻址到别处，从机不应答（OLED 传 `0x3C` → 实际寻址 `0x1E`，永远黑屏） | `OLED.c` 的 `#define OLED_I2C_ADDR 0x78`（7位 `0x3C` 左移）、`SHT30.c` 的 `SHT30_I2C_ADDR 0x88`（7位 `0x44` 左移） |
| **`I2C2_BusInit()` 是幂等的**                     | OLED 和 SHT30 都会调它，第二次是空操作。**不要去掉那个 `I2C2_Ready` 标志**，否则会把 PB10/PB11 重配成通用开漏，那一瞬总线浮空      | `I2C2.c`                                                                                                           |
| 改 I2C 速率只改一处                               | `I2C2_SPEED` 在 `I2C2.h`，两个器件共用                                                                                             | `I2C2.h`                                                                                                           |

I2C2 上挂了多个从机，地址互不冲突：**OLED `0x3C`、SHT30 `0x44`**（还可加 DS3231 `0x68`）。

### 4.3 延时与中断

| 坑                                                 | 后果                                                                                                                                                                           | 出处                                                                                                    |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| **`Delay` 硬编码 72MHz，不能用 `SystemCoreClock`** | 框架的 `SystemInit()` **从不调用 `SetSysClock()`**，`SystemCoreClock` 停在初始化的 `8000000`。用它算延时会让所有延时**短 9 倍**                                                | `Delay.c` 的 `#define DELAY_CLK_MHZ 72`，注释里写明了原因。**别"顺手优化"成 `SystemCoreClock/1000000`** |
| **ISR 里绝对、绝对不要调 `Delay`**                 | `Delay.c` 结尾会写 `SysTick->CTRL = 0`。主循环若正卡在 `while ((SysTick->CTRL & 0x00010000) == 0);` 等 COUNTFLAG，SysTick 被关掉后 COUNTFLAG **永远不会置位 → 主循环永久挂死** | `Delay.c` 最后一行                                                                                      |
| `Delay_us` 单次上限约 233ms                        | `SysTick->LOAD` 是 24 位，超了会静默截断成**更短**的延时                                                                                                                       | `Delay.c` 有溢出保护                                                                                    |
| `SysTick_Handler` 是空桩，且从不触发               | `Delay` 用轮询，`CTRL=0x05` 没开 `TICKINT`。**不要启用 SysTick 中断**，会和 `Delay` 打架                                                                                       | `src/stm32f10x_it.c`                                                                                    |

### 4.4 定时器与引脚

| 坑                                                                                 | 后果                                                                                                                                                                                                     | 出处                                                |
| ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| **`Key_Init()` 会清掉 `AFIO->MAPR` 低 24 位**                                      | `GPIO_Remap_SWJ_JTAGDisable` 走的 SPL 分支里有 `AFIO->MAPR &= 0xF0FFFFFF`，**副作用是抹掉所有外设重映射**。所以 **`Key_Init()` 必须放 `main()` 最后**，且**以后任何 `GPIO_PinRemapConfig` 都要在它之后** | `stm32f10x_gpio.c:548` 的 `DBGAFR_SWJCFG_MASK` 分支 |
| **PB3 是 JTDO，必须重映射**                                                        | 按键3 用了 PB3。需要 `GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable, ENABLE)`，且**必须在 `GPIO_Init` 之前**（JTAG 优先级高于 CRH/CRL）                                                                 | `Key.c`                                             |
| **AFIO 时钟必须先开**                                                              | `GPIO_PinRemapConfig` 直接写 `AFIO->MAPR`，时钟没开**写被静默丢弃**。`RCC_APB2Periph_AFIO` 要和 GPIO 时钟一起开                                                                                          | `Key.c`                                             |
| **绝不用 `GPIO_Remap_SWJ_Disable`（0x00300400）**                                  | 那是 "Full SWJ Disabled"，**连 SWD 一起关，ST-Link 再也连不上**。只能用 `GPIO_Remap_SWJ_JTAGDisable`（0x00300200，保留 SWD）                                                                             | `stm32f10x_gpio.h:195-197`                          |
| **`GPIO_Init` 掩码绝不能写 `GPIO_Pin_All`**                                        | 会重写掩码里每一位的 CNF/MODE，毁掉 PB10/PB11（I2C2 → OLED+SHT30 全挂）、PA0/PA1（ADC）、甚至 **PA13/PA14（SWD → 连不上）**。**逐个引脚调用**                                                            | `Key.c` 的引脚表循环                                |
| **TIM1 是高级定时器，不调 `TIM_CtrlPWMOutputs(TIM1, ENABLE)` 就没有任何 PWM 输出** | BDTR 的 MOE 位默认是关的。TIM2/3/4 **不需要**这步 —— 从通用定时器换过来极易卡住                                                                                                                          | 蜂鸣器驱动（待写）                                  |
| TIM1 挂在 **APB2**                                                                 | 要用 `RCC_APB2Periph_TIM1`，不是 APB1。用错能编译过但没波形                                                                                                                                              | 同上                                                |

### 4.5 ADC + DMA

| 坑                                                   | 后果                                                                                                                                            | 出处                                           |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| **`AD_Init()` 之后不能再碰 ADC**                     | `CNDTR`/`CMAR` 在 `EN=1` 时写保护，关掉通道也不复位。重启会让 ADC 从 rank1 开始而 DMA 从上次槽位继续 → **永久性通道互换**（烟雾值跑进光敏变量） | `AD.c` 的 `AD_Init` 注释里有完整的安全重启配方 |
| **`DMA_BufferSize` 必须严格等于 `ADC_NbrOfChannel`** | 这个相等关系是"数组下标 ↔ rank ↔ 通道"成立的唯一依据。写错会串位，**且不报任何错**                                                              | `AD.c`                                         |
| **`DMA_MemoryInc` 漏了 → 两路都写 `[0]`**            | 第二个通道永远读 0                                                                                                                              | `AD.c`                                         |
| **`AD_Value` 必须 `volatile`**                       | DMA 写内存是硬件行为，编译器看不见。不加会被优化成读寄存器里的陈旧值                                                                            | `AD.h` 有说明                                  |
| `ADC_SoftwareStartConvCmd` 只调一次                  | 之后连续转换永不停止。**它传 `DISABLE` 也停不掉**正在跑的连续转换，只有 `ADC_Cmd(DISABLE)` 能停                                                 | `AD.c`                                         |

### 4.6 中断与并发

| 坑                                                        | 后果                                                                                                                                                        | 出处                            |
| --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| **`TIM_ClearITPendingBit` 必须有**                        | F1 的 TIM 更新标志 `UIF` 只能软件清。漏了会让 NVIC 线持续有效，中断返回后立刻重新挂起 → **主循环永远回不去**。现象是"屏幕冻在最后一帧像死机，但 SWD 还能连" | `Key.c` 的 `TIM2_IRQHandler`    |
| **队列 `s_Queue` 必须保持 `volatile`**                    | 它保证"先写载荷、再发布头指针"这个发布顺序不被编译器重排。去掉后编译器可以把载荷挪到指针发布之后，消费者读到残缺槽位                                        | `Key.c` 的 `Key_PushEvent` 注释 |
| 队列满时丢**最新**，不是最旧                              | 丢最旧需要生产者推进 `s_Tail`（消费者的变量），会引入需要临界区的竞争，无锁性就没了                                                                         | 同上                            |
| **`NVIC_PriorityGroupConfig` 只能调一次，在 `main()` 里** | `NVIC_Init` 按**当前** `AIRCR.PRIGROUP` 算优先级位偏移，而 `NVIC->IP[]` 写进去不再重算。别的模块用不同分组再调一次，已写入的优先级**含义会静默改变**        | `main.c`                        |

### 4.7 显示

| 坑                                         | 后果                                                                                                                                 | 出处                                           |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| **`OLED_Font.h` 只能被 `OLED.c` 包含一次** | `OLED_F8x16` 是**带外部链接的暂定定义**（C 里文件作用域 `const` 不像 C++ 会隐式 static）。被两个编译单元包含就是**重复符号链接错误** | `OLED.h` 里**绝不能** `#include "OLED_Font.h"` |
| **SSD1315 保持页寻址模式**                 | 整份 init 命令表里**刻意不发 `0x20`**。SSD1315 在水平寻址模式下与 `0xB0` 混用时不跨页自动换行，页寻址从根上避开这个坑                | `OLED.c` 的 `OLED_InitCmd[]`                   |
| `OLED_DisplayBuf` 是 1KB 显存              | 绘图只写 RAM，必须调 `OLED_Update()` 才刷屏                                                                                          | `OLED.c`                                       |

---

## 5. 已知的正常现象（别去"修"）

- **构建有且只有一条警告**：

  ```
  lib\Hardware\OLED_Font.h:6:1: warning: missing braces around initializer [-Wmissing-braces]
  ```

  这是字库文件的扁平花括号写法，**数据完全正确**。改成嵌套花括号没有意义。

- **Flash 占用会波动**：链接器开了 `-ffunction-sections --gc-sections`，没被调用的函数连同它们拖进来的软浮点例程会被回收。例如撤掉传感器显示后，`SHT30_ReadData`/`OLED_ShowFloatNum`/`AD_GetVoltage` 会被 GC，flash 从 9.6KB 降到 6.8KB。**恢复显示后会长回去约 2.8KB，这是正常的。**

- **`pio run` 显示 `Took 1.xx seconds` 且体积变了**：说明只重编了改动的文件。要看全量结果先 `-t clean`。

---

## 6. 引脚与外设分配

### 引脚表

| 模块      | 外设          | 引脚                                     | 备注                                  |
| --------- | ------------- | ---------------------------------------- | ------------------------------------- |
| OLED      | I2C2          | PB10(SCL) / PB11(SDA)                    | 地址 `0x3C`                           |
| SHT30     | I2C2          | PB10 / PB11                              | 地址 `0x44`，与 OLED 共用总线         |
| MQ-2 烟雾 | ADC1_IN0      | PA0                                      | 5V 供电，经 10k/20k 分压（系数 ×1.5） |
| 光敏电阻  | ADC1_IN1      | PA1                                      | 3.3V 供电                             |
| 6 按键    | GPIO 上拉输入 | PB15 PA9 PB3 PB5 PB7 PB9（即按键1~6）    | 另一端接 GND，**按下=低**             |
| LED       | GPIO 推挽     | PB12                                     | **高电平点亮**（PB12→1k→LED正极→GND） |
| 蜂鸣器    | TIM1_CH3      | **PA10**                                 | 无源蜂鸣器（高电平触发），**待写**    |
| SD 卡     | SPI1          | **PA4(CS) PA5(SCK) PA6(MISO) PA7(MOSI)** | **待写**                              |
| SWD       | —             | PA13 / PA14                              | 调试用，**绝不能占用**                |

**空闲引脚**：PA2 PA3 PA8 PA11 PA12 PA15 PB0 PB1 PB2 PB4 PB6 PB8 PB13 PB14
其中 **PA2/PA3 建议留给 USART2 做串口日志**（`printf` 重定向）。

> ⚠️ **按键占用了这些脚的复用功能**：
> `PA9` = USART1_TX（**USART1 因此作废**）、`PB15` = TIM1_CH3N、
> `PB7` = TIM4_CH2、`PB9` = TIM4_CH4、`PB5` = TIM3_CH2。
>
> 另外 **PB12~PB15 一整片都只有 TIM1 的互补输出/刹车脚**
> （TIM1_CH1N / CH2N / CH3N / BKIN），**没有普通定时器通道**。
> 往那片挂 PWM 器件要小心 —— 蜂鸣器最初选 PB14 就是因为这个原因换掉的
> （PB14 只有 `TIM1_CH2N` 互补输出，得配 `TIM_OutputNState` + 死区配 0，
> 而且 duty 是反相的）。

### 外设分配

| 外设                 | 用途                             |
| -------------------- | -------------------------------- |
| I2C2                 | OLED + SHT30（+ 未来 DS3231）    |
| ADC1 + DMA1_Channel1 | MQ-2 + 光敏，连续扫描 + 循环搬运 |
| TIM2                 | 按键 10ms 扫描中断               |
| TIM1_CH3             | 蜂鸣器 PWM（待写）               |
| SPI1                 | SD 卡（待写）                    |
| SysTick              | `Delay` 独占（轮询，不开中断）   |

**空闲**：SPI2 USART2 USART3 I2C1 TIM3 TIM4 ADC2 RTC IWDG WWDG CAN USB
（**USART1 已作废** —— 它的 TX 脚 PA9 被按键占用；串口日志走 USART2）

> 注意 `SPI2`（MOSI 脚 **PB15 是按键1**）和 `USART3`（PB10/PB11 是 I2C2）
> **都被堵死了**，别选。（SPI2 另外两个脚 PB13/PB14 现在空闲，
> 但缺 MOSI 就凑不齐一组，除非把按键1 挪走。）

---

## 7. 模块结构

```
lib/Hardware/          每个模块一对 .c/.h
├── I2C2.c/h           总线层：OLED 和 SHT30 共用（幂等初始化 + 死锁恢复 + 读写）
├── OLED.c/h           显示层：帧缓冲 + 字符/数字/图形 API
├── OLED_Font.h        8x16 字库，95 个字符（0x20~0x7E）。只被 OLED.c 包含
├── SHT30.c/h          温湿度，走 I2C2
├── AD.c/h             ADC1+DMA 双通道模拟量（注意是 AD_ 不是 ADC_）
├── Key.c/h            6 按键：TIM2 中断扫描 + 消抖 + 5状态机 + 无锁环形队列
├── LED.c/h            PB12 单 LED
└── Delay.c/h          SysTick 轮询延时（自己拥有 SysTick）
lib/SPL/               vendored ST 标准外设库，**不要改**
src/main.c             启动顺序 + 应用逻辑
```

**分层关系**：`I2C2` 是最底层总线，`OLED` 和 `SHT30` 都建在它上面。加新的 I2C 器件时，
**直接用 `I2C2_Write/Read/WriteRead`，不要自己写 I2C 时序**。

---

## 8. 验证习惯

1. 改完**必须** `pio run`，且除第 5 节那条已知警告外**无新警告**。
2. 涉及**中断、引脚、时钟**的改动，要在回复里说清**上板怎么验证**（我不能看到硬件）。
3. 改完核对符号落位：

   ```bash
   "$NM" -S --size-sort .pio/build/genericSTM32F103C8/firmware.elf | grep 你的模块
   ```

   RAM 变量应在 `.bss`（地址 `0x200000xx`），常量应在 `.rodata`（地址 `0x0800xxxx`）。
4. **不要一次接一堆新硬件再上电**，逐个点亮。

---

## 9. 当前进度与未完成事项

### 已完成（**都上板验证过**）

- I2C2 总线层（400kHz，带超时保护、死锁恢复、幂等初始化）
- OLED 显示（SSD1315，128x64，1KB 帧缓冲，页寻址）
- SHT30 温湿度（CRC-8 校验，`0x2400` 禁止时钟拉伸）
- ADC1 + DMA 双通道模拟量（连续扫描 + 循环搬运）
- 6 按键（TIM2 中断扫描 + 状态机：单击/双击/长按/长按重复）
- LED

### 未完成

| #   | 事项                                 | 状态                 | 关键约束                                                                                                                                                               |
| --- | ------------------------------------ | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **蜂鸣器 PWM 音调**                  | 等货                 | 无源蜂鸣器**高电平触发**模块；**PA10 / TIM1_CH3**；**必须 `TIM_CtrlPWMOutputs(TIM1, ENABLE)`**；`TIM_OC3Init` + `TIM_SetCompare3`；**非阻塞**（不能 `Delay` 卡主循环） |
| 2   | **SD 卡 + FATFS 数据记录**           | 等货                 | SPI1（PA4/PA5/PA6/PA7）；格式 `时间戳,温度,湿度,烟雾,光照`                                                                                                             |
| 3   | **IWDG 看门狗**                      | **可立刻做，不用买** | 教程 14-1。做完"工业级"才有实际支撑                                                                                                                                    |
| 4   | 串口日志（USART2 + `printf` 重定向） | 未开始               | 建议占 PA2/PA3                                                                                                                                                        |
| 5   | 低功耗模式（停机/待机）              | 未开始               | 教程 13-x                                                                                                                                                              |
| 6   | 多级菜单                             | 未开始               | 按键状态机已为此打好基础                                                                                                                                               |
| 7   | **恢复传感器显示**                   | 待做                 | 调试期为按键界面腾位置撤下了。**恢复后 flash 会长约 2.8KB**                                                                                                            |

### ⚠️ 当前 OLED 显示的是**按键调试界面**，不是环境数据

调试阶段 `src/main.c` 显示的是按键状态（计数/标签/实时状态/最近事件），
**传感器采集仍在运行**（`SHT30_Init()`、`AD_Init()` 都还在调），只是没有显示。
恢复显示时注意 flash 会长回去（见第 5 节）。
